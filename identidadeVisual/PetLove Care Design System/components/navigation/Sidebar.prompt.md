Admin desktop shell rail — navy gradient, white lockup, emoji-led nav (active = coral pill), admin user block with logout.

```jsx
<Sidebar active="agendamentos" adminName="Gabriel"
         onSelect={setView} onLogout={sair} />
```

Icons follow the codebase convention (emoji): 📅 Agendamentos · ✂️ Serviços · 💬 Feedbacks.
