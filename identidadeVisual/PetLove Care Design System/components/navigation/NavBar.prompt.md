Marketing site top bar — brand wordmark, centered nav links (active gets a coral underline), coral "Agende" CTA.

```jsx
<NavBar active="Serviços" onAgendar={openModal}
        onNavigate={(l) => goTo(l)} />
```
