Brand text field with optional label + error; coral focus ring. Use `filled` over navy modals, `light` on admin forms.

```jsx
<Input label="CPF" placeholder="000.000.000-00" theme="filled" />
<Input label="Preço" type="number" theme="light" error="Campo obrigatório" />
```
