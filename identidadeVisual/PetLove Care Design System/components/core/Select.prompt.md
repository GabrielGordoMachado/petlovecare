Brand dropdown used in the scheduling modal and admin forms; CSS chevron, coral focus ring.

```jsx
<Select label="Escolha o pet" placeholder="Escolha o pet"
        options={['Rex', 'Mia', 'Thor']} theme="filled" />
```
