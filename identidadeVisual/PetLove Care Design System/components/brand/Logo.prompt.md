PetLove Care brand mark — paw-heart with a cat silhouette. Use `white` theme on navy surfaces, `color` on light.

```jsx
<Logo variant="lockup" theme="color" height={90} />
<Logo variant="mark" theme="white" height={48} />     {/* sidebar / splash */}
<Logo variant="wordmark" theme="color" height={40} /> {/* nav bar */}
```

Set `basePath` if the page isn't two levels deep from the project root.
