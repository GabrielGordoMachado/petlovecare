services:
  db:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_USER: petlovecare
      POSTGRES_PASSWORD: senha123
      POSTGRES_DB: petlovecare
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
