# URL base da API (NestJS). Ver GUIA_INTEGRACAO.md.
# Produção (Railway) — padrão recomendado para desenvolver as telas:
VITE_API_URL=https://petlovecare-api-production.up.railway.app
# Local (só se você sobe a API na sua máquina):
# VITE_API_URL=http://localhost:3000
